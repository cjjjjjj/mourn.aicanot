version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/app/data
      - NODE_ENV=production
    volumes:
      - mourn-data:/app/data
    restart: always

volumes:
  mourn-data:
    driver: local
